<h1>Create New Heroine: </h1>
<%= form_for @heroine do |f| %>
  <%= f.label :real_name %>
  <%= f.text_field :name %><br />

  <%= f.label :super_name %>
  <%= f.text_field :super_name %><br />

  <%= collection_select :heroine, :power_id, Power.all, :id, :name%>

  <%= f.submit %>
<% end %>